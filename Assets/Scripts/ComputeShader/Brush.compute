#include "Utils.hlsl"
#pragma kernel CSMain

RWTexture2D<float4> Result;
int seed;

uint kind;
uint2 pos;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    if(distance(id,pos)<10)
        Result[id.xy]=kind2Value(kind);
}